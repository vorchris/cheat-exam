const config = {
  workdirectory : "/home/student/SHARE",
  tempdirectory : "/tmp",
  debug: false,
  port: 3000,
  httpPort: 8000,
  development: {
  },
  production: {
  }
}
module.exports = config
